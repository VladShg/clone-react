import { styled } from '@mui/material'

const Name = styled('div')(({ theme }) => ({
	color: theme.palette.common.black,
	fontSize: '15px',
	fontWeight: 'bold',
	lineHeight: '20px',
}))

export default Name
